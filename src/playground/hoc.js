// A component that renders another components


import React from "react";
import  ReactDOM  from "react-dom";

const Info = (props) => {
    <div>
        <h1>Info</h1>
        <p>The Info is : {props.info}</p>
    </div>
}

